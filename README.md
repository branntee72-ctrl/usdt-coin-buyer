# USDT-to-Altcoin Buyer

This project provides a simple Python script to swap **USDT** for low-value/low-cap coins using the Binance API.

## Features
- **Price Tracking**: Monitor prices of specific low-value coins.
- **Automated Buying**: Set a USDT amount and target coin to execute a market buy.
- **Safety**: Uses a `config.py` file to keep API keys secure.

## Prerequisites
- A [Binance Account](https://binance.com) with USDT.
- API Key and Secret Key from your exchange settings.

## Setup
1. Clone this repo: `git clone https://github.com`
2. Install dependencies: `pip install python-binance`
3. Update `config.py` with your credentials.
